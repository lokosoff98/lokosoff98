"use strict";

(function() {

	// var cloneElem = function(dataClone) {
	// 	var elemApp = document.querySelector("#template").content.querySelector(".elem").cloneNode(true);
	// 		elemApp.children[0].textContent = dataClone;
	// 		document.body.appendChild(elemApp);
	// }

	// window.jsonData = function(dataNumber) {
	// 	var dataPars = JSON.parse(dataNumber);
	// 	for (var i = 0; i < dataPars.length; i++){
	// 		cloneElem(dataPars[i]);
	// 	}
	// };

	// var loader = document.createElement("script");
	// loader.src = "js/script-callback.js";
	// document.body.append(loader);

	var xhr = new XMLHttpRequest();

	console.log(xhr);
})();

